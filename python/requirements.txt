# Runtime dependencies for pymetkit.
findlibs~=0.1.3

# Required by ParamDB (pymetkit.paramdb / pymetkit.models), imported at package
# import time. Keep in sync with install_requires in cmake/pymetkit_setup.*.in.
pydantic>=2
PyYAML>=5.1
requests
