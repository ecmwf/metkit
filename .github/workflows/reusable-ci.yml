name: reusable-ci

on:
  workflow_call:
    inputs:
      metkit_sha:
        required: false
        type: string
      eccodes:
        required: false
        type: string
      eckit:
        required: false
        type: string

jobs:
  ci:
    name: metkit-ci
    uses: ecmwf-actions/reusable-workflows/.github/workflows/ci.yml@v2
    with:
      repository: ecmwf/metkit
      ref: ${{ inputs.metkit_sha }}
      name_prefix: metkit-
      build_package_inputs: |
        repository: ecmwf/metkit
        sha: ${{ inputs.metkit_sha }}
        self_coverage: true
        dependencies: |
          ecmwf/ecbuild
          MathisRosenhauer/libaec@master
          ${{ inputs.eccodes || 'ecmwf/eccodes' }}
          ${{ inputs.eckit || 'ecmwf/eckit' }}
        dependency_branch: develop
        parallelism_factor: 8
    secrets: inherit
